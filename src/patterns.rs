use crate::grid::get;
use crate::solve_grid::*;
use crate::solve_grid::SolveGrid;

/// Pattern finders? TODO:

pub fn is_one_two_one(solve_state: &SolveState, i: usize, j: usize) -> bool {
    // get(solve_grid, i, j);
    false
}
pub fn is_one_two_two_one(solve_state: &SolveState, i: usize, j: usize) -> bool {
    // get(solve_grid, i, j);
    false
}

pub fn is_one_one(solve_state: &SolveState, i: usize, j: usize) -> bool {
    // get(solve_grid, i, j);
    false
}
